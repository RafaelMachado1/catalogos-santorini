export type ThemeMode = 'light' | 'dark'

export type ThemeTokens = {
  background: string
  surface: string
  surfaceAlt: string
  text: string
  heading: string
  muted: string
  border: string
  accent: string
}
